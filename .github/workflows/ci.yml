name: CI Pipeline

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print a Debug Message
        run: echo "Hello, GitHub Actions! This is a test log."

      - name: List Files in the Repo
        run: ls -lah

